import type { DebugQuestion } from "../types/assessment";

export const ROUND2_DURATION_MS = 60 * 60 * 1000;

export const DEBUG_QUESTIONS: DebugQuestion[] = [
  {
    id: "debug-reverse-array",
    index: 1,
    title: "Reverse an Array In-Place",
    difficulty: "Easy",
    statement:
      "Given an array of integers, reverse the array in place. The left and right pointers must move toward each other, and the function must not read or write outside the array bounds.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "void reverseArray(vector<int>& arr) {",
        "    int left = 0;",
        "    int right = arr.size();",
        "    while (left < right) {",
        "        swap(arr[left], arr[right]);",
        "        left++;",
        "        right--;",
        "    }",
        "}",
      ],
      Python: [
        "def reverse_array(arr):",
        "    left = 0",
        "    right = len(arr)",
        "    while left < right:",
        "        arr[left], arr[right] = arr[right], arr[left]",
        "        left += 1",
        "        right -= 1",
      ],
      Java: [
        "public class Solution {",
        "    public void reverseArray(int[] arr) {",
        "        int left = 0;",
        "        int right = arr.length;",
        "        while (left < right) {",
        "            int tmp = arr[left];",
        "            arr[left] = arr[right];",
        "            arr[right] = tmp;",
        "            left++;",
        "            right--;",
        "        }",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "5\n1 2 3 4 5", output: "5 4 3 2 1" },
      { input: "1\n42", output: "42" },
    ],
    bugHint: "Watch the initial value of the right pointer.",
  },
  {
    id: "debug-find-max",
    index: 2,
    title: "Find the Maximum Element",
    difficulty: "Medium",
    statement:
      "Given an array of integers, return the largest element. The result must be correct even when every element is negative.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "int findMax(const vector<int>& arr) {",
        "    int best = 0;",
        "    for (int i = 1; i < arr.size(); i++) {",
        "        if (arr[i] > best) best = arr[i];",
        "    }",
        "    return best;",
        "}",
      ],
      Python: [
        "def find_max(arr):",
        "    best = 0",
        "    for i in range(1, len(arr)):",
        "        if arr[i] > best:",
        "            best = arr[i]",
        "    return best",
      ],
      Java: [
        "public class Solution {",
        "    public int findMax(int[] arr) {",
        "        int best = 0;",
        "        for (int i = 1; i < arr.length; i++) {",
        "            if (arr[i] > best) best = arr[i];",
        "        }",
        "        return best;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "4\n3 7 2 9", output: "9" },
      { input: "4\n-10 -20 -30 -40", output: "-10" },
    ],
    bugHint: "What does the function return when every element is negative?",
  },
  {
    id: "debug-sum-evens",
    index: 3,
    title: "Sum of Even Values",
    difficulty: "Easy",
    statement:
      "Given an array of integers, return the sum of all even values in the array.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "int sumEven(const vector<int>& arr) {",
        "    int total = 0;",
        "    for (int i = 0; i < arr.size(); i++) {",
        "        if (i % 2 == 0) total += arr[i];",
        "    }",
        "    return total;",
        "}",
      ],
      Python: [
        "def sum_even(arr):",
        "    total = 0",
        "    for i in range(len(arr)):",
        "        if i % 2 == 0:",
        "            total += arr[i]",
        "    return total",
      ],
      Java: [
        "public class Solution {",
        "    public int sumEven(int[] arr) {",
        "        int total = 0;",
        "        for (int i = 0; i < arr.length; i++) {",
        "            if (i % 2 == 0) total += arr[i];",
        "        }",
        "        return total;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "6\n1 2 3 4 5 6", output: "12" },
      { input: "2\n2 4", output: "6" },
    ],
    bugHint: "Should the parity check inspect the value or the index?",
  },
  {
    id: "debug-fibonacci",
    index: 4,
    title: "Nth Fibonacci Number",
    difficulty: "Medium",
    statement:
      "Return the nth Fibonacci number, where fib(0) = 0 and fib(1) = 1. Each subsequent value is the sum of the two previous ones.",
    starters: {
      "C++": [
        "int fib(int n) {",
        "    if (n <= 1) return n;",
        "    int a = 0, b = 1;",
        "    for (int i = 0; i < n; i++) {",
        "        int next = a + b;",
        "        a = b;",
        "        b = next;",
        "    }",
        "    return a;",
        "}",
      ],
      Python: [
        "def fib(n):",
        "    if n <= 1:",
        "        return n",
        "    a, b = 0, 1",
        "    for _ in range(n):",
        "        a, b = b, a + b",
        "    return a",
      ],
      Java: [
        "public class Solution {",
        "    public int fib(int n) {",
        "        if (n <= 1) return n;",
        "        int a = 0, b = 1;",
        "        for (int i = 0; i < n; i++) {",
        "            int next = a + b;",
        "            a = b;",
        "            b = next;",
        "        }",
        "        return a;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "5", output: "5" },
      { input: "10", output: "55" },
    ],
    bugHint: "Count exactly how many times the loop runs.",
  },
  {
    id: "debug-palindrome",
    index: 5,
    title: "Palindrome Check",
    difficulty: "Easy",
    statement:
      "Given a string, return true if it reads the same forwards and backwards. The two pointers must never access an index outside the string.",
    starters: {
      "C++": [
        "#include <string>",
        "using namespace std;",
        "",
        "bool isPalindrome(string s) {",
        "    int left = 0;",
        "    int right = s.length();",
        "    while (left < right) {",
        "        if (s[left] != s[right]) return false;",
        "        left++;",
        "        right--;",
        "    }",
        "    return true;",
        "}",
      ],
      Python: [
        "def is_palindrome(s):",
        "    left = 0",
        "    right = len(s)",
        "    while left < right:",
        "        if s[left] != s[right]:",
        "            return False",
        "        left += 1",
        "        right -= 1",
        "    return True",
      ],
      Java: [
        "public class Solution {",
        "    public boolean isPalindrome(String s) {",
        "        int left = 0;",
        "        int right = s.length();",
        "        while (left < right) {",
        "            if (s.charAt(left) != s.charAt(right)) return false;",
        "            left++;",
        "            right--;",
        "        }",
        "        return true;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "racecar", output: "true" },
      { input: "hello", output: "false" },
    ],
    bugHint: "An array's last valid index is length - 1.",
  },
  {
    id: "debug-missing-number",
    index: 6,
    title: "Find the Missing Number",
    difficulty: "Medium",
    statement:
      "Given an array of size n that holds all but one of the numbers from 1 to n, return the missing number.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "int missingNumber(const vector<int>& arr, int n) {",
        "    int expected = (n * (n + 1)) / 2;",
        "    int actual = 0;",
        "    for (int i = 0; i <= n; i++) {",
        "        actual += arr[i];",
        "    }",
        "    return expected - actual;",
        "}",
      ],
      Python: [
        "def missing_number(arr, n):",
        "    expected = (n * (n + 1)) // 2",
        "    actual = 0",
        "    for i in range(n + 1):",
        "        actual += arr[i]",
        "    return expected - actual",
      ],
      Java: [
        "public class Solution {",
        "    public int missingNumber(int[] arr, int n) {",
        "        int expected = (n * (n + 1)) / 2;",
        "        int actual = 0;",
        "        for (int i = 0; i <= n; i++) {",
        "            actual += arr[i];",
        "        }",
        "        return expected - actual;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "5\n2 3 1 5", output: "4" },
      { input: "3\n1 3", output: "2" },
    ],
    bugHint: "The loop reads one element beyond the last index.",
  },
  {
    id: "debug-sorted-array",
    index: 7,
    title: "Check Sorted Ascending",
    difficulty: "Easy",
    statement:
      "Given an array of integers, return true if it is sorted in non-decreasing order. The check must never read past the last element.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "bool isSorted(const vector<int>& arr) {",
        "    for (int i = 0; i < arr.size(); i++) {",
        "        if (arr[i] > arr[i + 1]) return false;",
        "    }",
        "    return true;",
        "}",
      ],
      Python: [
        "def is_sorted(arr):",
        "    for i in range(len(arr)):",
        "        if arr[i] > arr[i + 1]:",
        "            return False",
        "    return True",
      ],
      Java: [
        "public class Solution {",
        "    public boolean isSorted(int[] arr) {",
        "        for (int i = 0; i < arr.length; i++) {",
        "            if (arr[i] > arr[i + 1]) return false;",
        "        }",
        "        return true;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "4\n1 2 3 4", output: "true" },
      { input: "4\n1 3 2 4", output: "false" },
    ],
    bugHint: "On the last iteration the comparison reads past the final element.",
  },
  {
    id: "debug-count-greater",
    index: 8,
    title: "Count Greater Than Threshold",
    difficulty: "Easy",
    statement:
      "Given an array and a threshold, return the count of elements that are strictly greater than the threshold. Equal elements must not be counted.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "int countGreater(const vector<int>& arr, int t) {",
        "    int count = 0;",
        "    for (int x : arr) {",
        "        if (x >= t) count++;",
        "    }",
        "    return count;",
        "}",
      ],
      Python: [
        "def count_greater(arr, t):",
        "    count = 0",
        "    for x in arr:",
        "        if x >= t:",
        "            count += 1",
        "    return count",
      ],
      Java: [
        "public class Solution {",
        "    public int countGreater(int[] arr, int t) {",
        "        int count = 0;",
        "        for (int x : arr) {",
        "            if (x >= t) count++;",
        "        }",
        "        return count;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "4\n1 5 5 9\n5", output: "2" },
      { input: "3\n1 5 9\n9", output: "0" },
    ],
    bugHint: "Does the problem ask for greater than or greater than or equal?",
  },
  {
    id: "debug-second-largest",
    index: 9,
    title: "Find the Second Largest",
    difficulty: "Hard",
    statement:
      "Given an array with at least two distinct values, return the second largest value. The running second-largest must only update when a candidate actually beats the current value.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "int secondLargest(const vector<int>& arr) {",
        "    int first = arr[0];",
        "    int second = 0;",
        "    for (int i = 1; i < arr.size(); i++) {",
        "        if (arr[i] > first) {",
        "            second = first;",
        "            first = arr[i];",
        "        } else {",
        "            second = arr[i];",
        "        }",
        "    }",
        "    return second;",
        "}",
      ],
      Python: [
        "def second_largest(arr):",
        "    first = arr[0]",
        "    second = 0",
        "    for i in range(1, len(arr)):",
        "        if arr[i] > first:",
        "            second = first",
        "            first = arr[i]",
        "        else:",
        "            second = arr[i]",
        "    return second",
      ],
      Java: [
        "public class Solution {",
        "    public int secondLargest(int[] arr) {",
        "        int first = arr[0];",
        "        int second = 0;",
        "        for (int i = 1; i < arr.length; i++) {",
        "            if (arr[i] > first) {",
        "                second = first;",
        "                first = arr[i];",
        "            } else {",
        "                second = arr[i];",
        "            }",
        "        }",
        "        return second;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "4\n8 3 9 7", output: "8" },
      { input: "4\n3 5 2 1", output: "3" },
    ],
    bugHint: "The running second-largest is overwritten by every smaller element.",
  },
  {
    id: "debug-rotate-left",
    index: 10,
    title: "Rotate Array Left by One",
    difficulty: "Medium",
    statement:
      "Given an array, shift every element one position to the left and bring the first element to the back.",
    starters: {
      "C++": [
        "#include <vector>",
        "using namespace std;",
        "",
        "void rotateLeft(vector<int>& arr) {",
        "    int first = arr[0];",
        "    for (int i = 0; i < (int)arr.size() - 1; i++) {",
        "        arr[i] = arr[i + 1];",
        "    }",
        "    arr[0] = first;",
        "}",
      ],
      Python: [
        "def rotate_left(arr):",
        "    first = arr[0]",
        "    for i in range(len(arr) - 1):",
        "        arr[i] = arr[i + 1]",
        "    arr[0] = first",
      ],
      Java: [
        "public class Solution {",
        "    public void rotateLeft(int[] arr) {",
        "        int first = arr[0];",
        "        for (int i = 0; i < arr.length - 1; i++) {",
        "            arr[i] = arr[i + 1];",
        "        }",
        "        arr[0] = first;",
        "    }",
        "}",
      ],
    },
    sampleCases: [
      { input: "5\n1 2 3 4 5", output: "2 3 4 5 1" },
      { input: "3\n7 8 9", output: "8 9 7" },
    ],
    bugHint: "Where should the saved first element be placed at the end?",
  },
];

export const TOTAL_DEBUG_QUESTIONS = DEBUG_QUESTIONS.length;